document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-search]').forEach(input=>{const box=input.parentElement.querySelector('.search-results');if(!box)return;input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();if(q.length<2){box.classList.remove('open');return}const rows=PRODUCTS.filter(p=>[p.name,p.category,p.gauge,p.thickness,p.finish,p.profile,p.brand,p.availability,String(p.price),...p.colours].join(' ').toLowerCase().includes(q)).slice(0,7);box.innerHTML=rows.map(p=>`<a class="search-item" href="product-details.html?id=${p.id}"><img src="images/${p.id}.svg" alt=""><span><strong>${esc(p.name)}</strong><br><small>${money(p.price)} / ${p.unit}</small></span></a>`).join('')||'<div class="card-pad">No matching products.</div>';box.classList.add('open')});input.addEventListener('keydown',e=>{if(e.key==='Escape')box.classList.remove('open')})})});document.addEventListener('DOMContentLoaded',()=>{const q=new URLSearchParams(location.search).get('q');if(!q)return;const input=document.querySelector('[data-search]');if(input){input.value=q;input.dispatchEvent(new Event('input',{bubbles:true}));input.scrollIntoView({block:'center'})}});
